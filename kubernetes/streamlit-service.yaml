apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yaml
    kompose.version: 1.26.1 (HEAD)
  creationTimestamp: null
  labels:
    app: streamlit
  name: streamlit
spec:
  type: LoadBalancer
  ports:
  - port: 8501
  selector:
    app: streamlit
